import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ch.axa.innoday",
  appName: "AXA Innoday mobile app",
  webDir: "build",
  plugins: {},
};

export default config;
